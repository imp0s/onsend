{
  "name": "email-safety-checker",
  "version": "0.1.0",
  "description": "Outlook on-send add-in that blocks attachments and enforces same-domain recipients.",
  "scripts": {
    "test": "jest",
    "lint": "eslint . --ext .js"
  },
  "devDependencies": {
    "eslint": "^8.57.0",
    "eslint-config-prettier": "^9.1.0",
    "eslint-plugin-prettier": "^5.2.1",
    "jest": "^29.7.0",
    "prettier": "^3.3.3"
  }
}
