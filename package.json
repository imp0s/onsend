{
  "name": "onsend-cleaner",
  "version": "0.1.0",
  "description": "Outlook on-send add-in that cleans Word attachment metadata and comments.",
  "scripts": {
    "build": "esbuild src/addin.js --bundle --platform=browser --format=iife --global-name=WordAttachmentCleaner --outfile=public/app.js",
    "test": "jest",
    "lint": "eslint . --ext .js"
  },
  "dependencies": {
    "fast-xml-parser": "^4.3.5",
    "jszip": "^3.10.1"
  },
  "devDependencies": {
    "eslint": "^8.57.0",
    "eslint-config-prettier": "^9.1.0",
    "eslint-plugin-prettier": "^5.2.1",
    "esbuild": "^0.23.0",
    "jest": "^29.7.0",
    "prettier": "^3.3.3"
  }
}